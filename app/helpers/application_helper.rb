module ApplicationHelper
  # def paginate(scope, options = {})
  #   options[:theme] = 'bootstrap4' unless options.key?(:theme)
  #   options[:views_prefix] = 'kaminari' unless options.key?(:views_prefix)
  #   paginator = super(scope, options)
  #   paginator
  # end
end
